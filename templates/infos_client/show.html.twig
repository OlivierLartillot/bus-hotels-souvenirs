{% extends 'base.html.twig' %}

{% block title %}InfosClient{% endblock %}

{% block body %}
    <h1>InfosClient</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ infos_client.id }}</td>
            </tr>
            <tr>
                <th>Day</th>
                <td>{{ infos_client.day ? infos_client.day|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ infos_client.name }}</td>
            </tr>
            <tr>
                <th>NumberPersons</th>
                <td>{{ infos_client.numberPersons }}</td>
            </tr>
            <tr>
                <th>RoomNumber</th>
                <td>{{ infos_client.roomNumber }}</td>
            </tr>
            <tr>
                <th>WhatsAppNumber</th>
                <td>{{ infos_client.whatsAppNumber }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_infos_client_index') }}">back to list</a>

    <a href="{{ path('app_infos_client_edit', {'id': infos_client.id}) }}">edit</a>

    {{ include('infos_client/_delete_form.html.twig') }}
{% endblock %}
